/**
 * Application constants for casino management app
 */

/**
 * Default number of seats for a new table
 */
export const DEFAULT_SEATS_COUNT = 10;

/**
 * Default purchase limit for chip purchases
 */
export const DEFAULT_PURCHASE_LIMIT = 100;

/**
 * Minimum allowed purchase limit
 */
export const MIN_PURCHASE_LIMIT = 1;

/**
 * Maximum allowed purchase limit
 */
export const MAX_PURCHASE_LIMIT = 500;

/**
 * Minimum number of seats for a table
 */
export const MIN_SEATS_COUNT = 1;

/**
 * Maximum number of seats for a table
 */
export const MAX_SEATS_COUNT = 10;

/**
 * Working day start hour (24-hour format)
 */
export const WORKING_DAY_START_HOUR = 10;

/**
 * Working day end hour (24-hour format)
 */
export const WORKING_DAY_END_HOUR = 22;

/**
 * Minimum password length
 */
export const MIN_PASSWORD_LENGTH = 4;

/**
 * Preset chip amounts for quick actions
 */
export const CHIP_PRESETS = [-100, -500, -1000, -5000, 100, 500, 1000, 5000];

/**
 * Success message display duration in milliseconds
 */
export const SUCCESS_MESSAGE_DURATION = 2500;

/**
 * Maximum error message length to display
 */
export const MAX_ERROR_MESSAGE_LENGTH = 200;
